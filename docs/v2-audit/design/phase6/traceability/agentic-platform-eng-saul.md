# Traceability — "Agentic Platform Engineering" (saul / Fernandez) → V2 design

Source pass-3: `passes/agentic-platform-eng-saul/pass3-apply.md` (with pass-2 §F/§G/§H caveats carried).
Each distinct gap/insight from pass-3 §(b) REAL gaps + load-bearing §(c) caveats, classified against the
V2 design corpus. Every classification grounded by grep, not assumed.

Legend: **APPLIED** = carried into a MOVE / memory model / enforcement sort / distribution / loop / ledger.
**CUT** = consciously rejected (MASTER §F) or deferred (MASTER §C / RECON §F) with a stated reason.
**DROPPED** = not in synthesis, not in design, not consciously cut — a real miss.

---

## Per-gap table

| # | Gap / insight (pass-3) | Classification | Where it lands / why | Missing nuance (DROPPED sub-item) |
|---|---|---|---|---|
| b1 | **No cumulative / directory-scoped context loading (scoping).** Context docs flat at repo root; no parent→child cumulation, no file-pattern scoping. | **APPLIED** | The portable core ("scoping, not the three-repo split", pass2 §B) IS the V2 memory model. `.claude/rules/<area>.md` shards with native `paths:` globs auto-load 1–2 per task instead of all 574 PITFALLS lines (`phase3/RECONCILIATION.md` §A/§E; `memory-model.md`; `MASTER-FINDINGS.md` §B MOVE 3). Pass-3's own note that phrase-keyed triggers are a §9 replace-item also lands in MOVE 4. | — |
| b2 | **No hard retry cap with defined human-handoff on agentic loops.** `/cr` is "Opus auto-fix" with no iteration ceiling and no REJECT/handoff tier. Pass-3 calls this "the article's single most valuable, model-independent contribution." | **APPLIED** | Carried verbatim as MOVE 1 payload (c): "retry-ceiling counter + REJECT/handoff tier [C1-G3]" hosted on the one Stop/PostToolUse hook surface (`MASTER-FINDINGS.md:38`; `cluster-findings-1.md` G3). Capability-confirmed the counter is a hook-maintained counter, not a tool-rewind (`capability-facts.md:22-23`). | The **REJECT/handoff *tier* on `/cr` specifically** is registered but **gated behind `/goal`** (MASTER §C: "blocked behind MOVE 2"). The retry *counter* is built; the *generalization to debug/refactor loops* (pass2 §E: "every agentic loop needs a hard cap") is named but not separately scheduled — only the `/cr` auto-fix instance is in MOVE 1. |
| b2-emp | **The "max-N" number is empirical and model-dependent** — pass-3 §(d) carves out a "narrow YES": verify retry-value decay on Opus-class models before encoding a fixed ceiling (Stripe's "2" was tuned on their stack). | **CUT (deferred, with reason)** | Registered as the one re-research thread: `cluster-findings-1.md:306-308` ("Retry-value decay on Opus-class models — the only empirical, model-dependent question"). It is gated, not encoded as a literal "2". Legitimate deferral. | — |
| b3 | **No machine-readable skill manifest / registry.** Skills inventoried only in prose; live drift (`/dev`,`/explain` on disk not in canon; `dep-update` empty stub; `/cr-feature` retired-but-referenced). | **APPLIED** | `harness-manifest.json` (generated by `gen-manifest.sh`, asserted by `scan-context.sh` drift CI) is the convergence gate's standing half (`distribution.md` §3b; `MASTER-FINDINGS.md` MOVE 5). The exact drift cases pass-3 cites are the §3a convergence checklist rows 1/2/6 (`distribution.md:247-253`). Pass-3's correction that `skills-lock.json` is a *confirmed phantom* is itself overturned on disk — it EXISTS (`phase45/RECONCILIATION.md` C1; `distribution.md:14`), and is reused as the manifest precedent (S1 in `REVIEWER-CONSOLIDATION.md`). | — |
| b4 | **No cross-project / installable harness (the three-repo brain → our central V2 fact).** Never installed beyond event-vendor; no global `~/.claude/CLAUDE.md`. | **APPLIED** | This IS MOVE 5 / the whole Phase 4 distribution design: extract `agent-harness`, ship as plugin+marketplace, validate on 3 installs (`MASTER-FINDINGS.md` MOVE 5; `distribution.md` §1/§5; `V1-TO-V2-CARRYFORWARD.md` §B). | Pass-3's §(c) caveat that **Gap 4 is *lower-leverage than it looks*** (the three-repo brain is "the cheapest, least load-bearing part"; the value is in infra) is carried in spirit (distribution is honestly framed as "pure budget-(2), does not make the harness smaller", `distribution.md:414`), but the design does NOT down-rank installability relative to b2/firewall the way pass2 §A/§D argue — it remains the headline V2 thesis. Minor framing tension, not a mechanism miss. |
| b4-c | **§(c) caveat: adopt versioned-copy-with-lock, NOT symlink-live** (the article picks the weaker side; symlinks resolve to HEAD not a validated SHA → recreates the canon's §7.8 install contradiction). | **APPLIED** | Carried as an explicit rejection (MASTER §F "Symlink-live install — use versioned-copy-with-lock") AND as the load-bearing property of the plugin route (`distribution.md` §4a: "the version pin IS the validated-SHA lock… why the plugin does not recreate the canon's own install contradiction"). Anti-dup gate re-confirms symlink-live rejected (`REVIEWER-CONSOLIDATION.md` §1). | — |
| b5 | **No isolation boundary equivalent to a devbox.** Worktrees + Tier-0 firewall = *credential* isolation, not *environment* isolation; agents still run on the same machine/filesystem. No sandbox/devbox component exists. | **CUT (split: literal rejected §F + concept deferred)** | The literal devbox/dev-container/VM/microVM/gVisor stack is **rejected-as-literal** with a stated reason: "wrong threat model at solo scale; container-escape caused none of the documented incidents" (`MASTER-FINDINGS.md` §F:177; `lens-anti-duplication.md:97`; `REVIEWER-CONSOLIDATION.md` §1). The *residue* that survives is "allowlist operations not destinations, enforce off the model" = the `block-dangerous-bash.sh` guard + egress carve-out (MOVE 2). Disposable-environment isolation **as a concept** is registered, not built (`cluster-findings-1.md:327`; pass-3 §d: "an implementation spike, not research"). | The page **over-credited** us on isolation (pass2 §H); pass-3 correctly narrows the gap to "no disposable compute environment, confirmed absent." That confirmed-absent residue is acknowledged but has **no positive design slot** — it's only handled by *rejecting* the heavy form. The honest "the gap is real but we choose not to close it at solo scale" disposition is implicit in §F, not stated as its own registered-deferred line. Borderline; counted as CUT because §F's reason covers it. |
| c-orch | **§(c) caveat (pass2 §F): explicit-invoke (library) vs pinned-blueprint (Minions) is the unmade orchestration decision** — opposite agency stances the article never reconciles; "is orchestration explicit-invoke or pinned-blueprint?" is the crux for a harness designer. | **CUT (deferred, partial) — with a DROPPED nuance** | The *autonomy* half is registered + decided-deferred: "Autonomous trigger front-door (bug→PR; harness can't be summoned) — gated on V2 deciding autonomy is in scope" (MASTER §C) and surfaced as decision D3 "recommended NO, keep hypothesis-gated" (`REVIEWER-CONSOLIDATION.md:160-165`). `/goal` (the blueprint-like loop primitive) is registered-blocked (MASTER §C). | The pass raises orchestration **agency stance** as an architectural fork (does the *system* sequence procedures, or does the human always invoke?). The V2 design only frames the narrower **"is autonomy in scope?"** question. It never explicitly poses **explicit-invoke-vs-pinned-blueprint as the enforcement-architecture choice** the way pass2 §F does — the design assumes the explicit-invoke library model throughout (skills never auto-load) without registering that assumption as a decision. This is the one place the pass's framing is genuinely **not represented** as a stated fork. |
| c-model | **§(c) caveat (pass2 §G/§H): "more elaborate ≠ better"; measure scaffolds against the Model-Capacity Audit keep/remove** — the article points toward *more* curated markdown; our map pre-authorizes *cuts*. | **APPLIED** | This is MOVE 4 (the §9 Model-Capacity re-audit on Opus 4.8 — "the deletion engine"), the explicit counter to the article's frame (`MASTER-FINDINGS.md` MOVE 4; §A "fewer files and more wiring"). The two-budget reframe (`phase3/RECONCILIATION.md` §A; D5) and the budget-simplicity lens running the deletion engine on the build set (`lens-budget-simplicity.md:128`) operationalize it. The pass's "Better = more elaborate is a liability signal" reading is the design's governing principle. | — |

---

## Summary counts

- **Total distinct gaps/insights raised:** 9 (5 §(b) REAL gaps + b2's empirical-research carve-out + 3 load-bearing §(c) caveats: install-method, orchestration-stance, model-capacity).
- **APPLIED:** 5 (b1 scoping, b2 retry-cap mechanism, b3 manifest, b4 installable, b4-c versioned-copy-with-lock, c-model deletion engine) — counted as 6 distinct rows; b4-c and b4 share the install thread. Reported as **6** applied rows.
- **CUT (legitimate, with reason):** 3 (b2-emp retry-number deferred to one re-research pass; b5 devbox rejected-as-literal §F + concept registered; c-orch autonomy deferred §C / D3).
- **DROPPED (real misses):** 1 hard miss + 2 sub-nuances on otherwise-applied gaps:
  1. **(hard) The explicit-invoke-vs-pinned-blueprint *agency-stance* fork (c-orch).** The design defers *autonomy* but never registers the orchestration-architecture choice itself; it silently assumes the explicit-invoke library model. Belongs in MASTER §H forks or the MOVE-1/`/goal` decision as a named assumption.
  2. **(sub-nuance, b2) Generalizing the hard retry cap to *all* agentic loops (debug/refactor), not just `/cr` auto-fix.** Pass2 §E's whole point is the cap is a control-theory primitive for every loop; MOVE 1 only instantiates it on the `/cr` Stop surface. Belongs in MOVE 1 / enforcement-sort as an explicit "applies to debug + refactor loops too" line.
  3. **(sub-nuance, b5) No positive registered-deferred line for "disposable compute environment."** The confirmed-absent residue is handled only by *rejecting* the heavy form in §F; the honest "real gap, consciously not closed at solo scale" disposition isn't stated as its own item.

---

## Notes on grounding

- Retry cap → MOVE 1 (c): `MASTER-FINDINGS.md:38`, `cluster-findings-1.md:53-58`, `capability-facts.md:22-23`.
- Manifest → `distribution.md:262-290` (§3b), convergence checklist `distribution.md:247-253` (§3a).
- Scoping → `phase3/RECONCILIATION.md` §A/§B/§E, `memory-model.md`, `memory-model-candidate-c.md`.
- Devbox rejected → `MASTER-FINDINGS.md:177` (§F), `lens-anti-duplication.md:97`, `cluster-findings-4.md:276-282` (R5).
- Symlink → versioned-copy → `MASTER-FINDINGS.md:185` (§F), `distribution.md:138,302`.
- Model-Capacity Audit → `MASTER-FINDINGS.md:85` (MOVE 4), `lens-budget-simplicity.md`.
- `skills-lock.json` is REAL on disk (pass-3's "confirmed phantom" claim is the stale one): `distribution.md:14`, `phase45/RECONCILIATION.md` C1.
