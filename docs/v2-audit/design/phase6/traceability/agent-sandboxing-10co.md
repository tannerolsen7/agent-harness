# Traceability — agent-sandboxing-10co

Pass-3 source: `passes/agent-sandboxing-10co/pass3-apply.md` (building on `pass2-penetrate.md`).
Each distinct gap/insight from pass-3 §(b) REAL gaps, plus load-bearing §(c) caveats and §(d) conclusions,
classified against the V2 design corpus. Every classification grounded by grep, with a citation.

| # | Pass-3 gap / insight (origin) | Classification | Where it lands / why | Missing nuance (if any) |
|---|---|---|---|---|
| 1 | **(b)(1)** No safety-floor bash guard against destructive SQL / deploys — article #12 (`DROP TABLE`/`TRUNCATE`/`DELETE`-without-`WHERE`; Replit Jul-2025). | **APPLIED** | Absorbed into `block-dangerous-bash.sh` (MOVE 2). `enforcement-sort.md:48` "destructive SQL via psql/`supabase … execute`/heredoc — R77 (DB half)"; `cluster-findings-4.md:28-29` cites this article by name as a concrete failure mode for G1; MASTER-FINDINGS §B MOVE 2 lists it as the "most-cited single gap." Scoped as part of the single unified guard (not a standalone hook), exactly per pass-3 §(d) instruction (avoid §6 phantom-proliferation). Sequencing lens ranks it the #1 first-slice build (`lens-sequencing-criticalpath.md:273`). | — |
| 2 | **(b)(1) caveat** Existing CLAUDE.md "destructive ops must have a rollback path" is advisory prose, not an enforced hook — so this is a genuine gap, not a dup. | **APPLIED** | The advisory→deterministic distinction is the spine of MASTER-FINDINGS §A and is preserved: `cluster-findings-4.md:34-36` repeats the exact "advisory prose, not an enforced hook" point; R94 (rollback-path rule) stays L3-judgment in `enforcement-sort.md:198` while the bash-observable destructive ops relocate to L1. | — |
| 3 | **(b)(2)** Egress filtering is a confirmed absence at every layer (#9 Privoxy/`pfctl` allowlist); the only control for the supply-chain/injection-exfil class credential topology can't touch. | **APPLIED (build-now half) + CUT/deferred (full-firewall half)** | Build-now half is in MOVE 2: "Egress / outbound control for unattended `/queue` — operation-granularity, not destination" (`MASTER-FINDINGS.md:68`); the bash-observable slice (`curl`/`wget`/`fetch` to non-allowlisted host) folds into `block-dangerous-bash.sh` (`enforcement-sort.md:52`). The **full proxy/`pfctl` firewall is consciously deferred** in §C: "Full egress firewall (proxy/pfctl) [C4-G2] — research-gated; the config-level carve-out is the now-half" (`MASTER-FINDINGS.md:123`). Reason matches the article's own low confidence ("maintenance difficult"). | — |
| 4 | **(d)** Bounded research question: does Claude Code's Seatbelt/sandbox on macOS expose a usable egress allowlist, making Privoxy/`pfctl` unnecessary (gates egress build-vs-buy)? | **APPLIED (registered as a bounded capability check)** | Captured verbatim as the gate for the egress build: `cluster-findings-4.md:360-363` FR4(a) "whether the built-in Bash/Seatbelt sandbox exposes a usable egress allowlist (would make a Privoxy/`pfctl` proxy unnecessary — gates the build-vs-buy half of G2)"; surfaces in MASTER-FINDINGS §G item 3 "default `/queue` egress profile" (`MASTER-FINDINGS.md:198`). Classified as "run the binary on disk," not a research spawn. | — |
| 5 | **(b)(4)** Migration credential is unresolved: no hook blocks `supabase db push` from an agent worktree; policy = "agent verifies against local stack; human applies to prod" (Pass 2-B2). | **APPLIED (the enforcement half)** + **DROPPED (the encoded policy/ADR half)** | The `supabase db push` block IS designed: it folds into `block-dangerous-bash.sh` deploy-command class — `enforcement-sort.md:54` "deploy commands (`vercel deploy --prod`, `supabase db push`, `gh release create`) — R77's irreversible external mutation class." **But** pass-3 §(d) explicitly calls for encoding the *policy* ("agent verifies against a throwaway local stack; a human applies to prod") as **"a decision and an ADR"** (`pass3-apply.md:133-134`). No corpus row encodes that migration-path policy or an ADR for it — see DROPPED below. | The policy "agent verifies migration against local stack, human applies to prod" + an ADR for it. |
| 6 | **(b)(3)** No per-task wall-clock timeout / runaway control for `/queue` (article #8, 45-min SIGTERM-and-commit); addresses Open-Q #5's "10 silent PRs" throughput problem. | **DROPPED (the timeout mechanism)** | The cluster touched this only to dismiss it: `cluster-findings-4.md:124` notes session-end is "the closest declared item to a timeout — but it does memory-capture, not timeout." MOVE 1's retry-ceiling counter (`MASTER-FINDINGS.md:38`) is a *step/retry* ceiling, not a *wall-clock* timeout — different mechanism. The scheduler/heartbeat deferral (§C `MASTER-FINDINGS.md:120`) is about *firing* runs, not *killing* a runaway one. `TaskStop` exists in the runtime but no design row wires a per-task SIGTERM. Pass-3 itself tiers this "operational/observability, not security" — but registering-not-building still requires a §C entry, and there is none. See DROPPED. | A per-task wall-clock timeout / SIGTERM-and-commit for runaway `/queue` agents (even if only registered-deferred, not built). |
| 7 | **(c)(1)/(d)** The microVM/OrbStack/container axis (article #6, #11) is the wrong threat model for a solo-dev-own-repo system; reject and document (container-escape caused none of the incidents). | **CUT (consciously rejected)** | §F reject-as-literal: "Dev-container/VM/microVM/gVisor/token-proxy stack — wrong threat model at solo scale; container-escape caused none of the documented incidents. Residue = 'allowlist operations not destinations, enforce off the model.'" (`MASTER-FINDINGS.md:177-179`). The anti-dup lens re-confirms it honored: `lens-anti-duplication.md:97`. Matches pass-3 §(d)'s "no research; reject for this system; document the rejection." | — |
| 8 | **(a)(2)/(c)(2)** Credential-topology thesis is correct but already better-answered on disk by the worktree prod-key firewall; the article's #1/#7 "scoped Supabase key" is a roles-engineering project, not a checkbox (Supabase has no native tier between RLS-anon and service-role). The article is *behind disk*. | **CUT (anti-phantom — already built) + APPLIED (residual gap)** | The prod-key firewall is in §E anti-phantom: "Tier-0 prod-key firewall (`worktree-create.sh`/`gen-local-env.sh`/`test-local.sh`) — a disk advance over canon" (`MASTER-FINDINGS.md:151-153`). So the article's #1/#7 are correctly NOT re-proposed (their instinct is already realized by a better mechanism). The **residual** credential gap the firewall does *not* close — the shared prod `.env.local` symlink the agent can still `Read` — is carried as a smaller gap: §D "per-worktree credential scoping [C4-G10]" (`MASTER-FINDINGS.md:138`) and `cluster-findings-4.md:153-163` G10. | — |
| 9 | **(c)(3)/(a)(1)** PR-as-gate is over-ranked — it sits downstream of pre-PR exfiltration; the `.cr-ok` chain is gitignored and never reaches CI (advisory at the CI layer). | **APPLIED** | The "`.cr-ok` never reaches CI / forgeable stop authority" finding is a top-cited MOVE-2 gap: "Relocate the forgeable stop authority to CI — `.cr-ok` is model-computed and never verified by CI (Node 8.5c)" (`MASTER-FINDINGS.md:57-59`); `enforcement-sort.md:61-67` resolution (b). The "regression-trust not correctness-trust / not a capability unlock" constraint (the article's downstream-of-exfil point, generalized) is carried as the MOVE-1 constraint (`MASTER-FINDINGS.md:44`). | — |

## Net

- **Total distinct gaps/insights surfaced by this pass-3:** 9 (4 from §b, the egress research Q from §d,
  and 4 load-bearing §c/§d/§a conclusions that should shape the design).
- **APPLIED:** 6 (gaps 1, 2, 3-build-half, 4-capability-check, 8-residual, 9; plus the enforcement half of 5).
- **CUT (consciously rejected §F or deferred §C, with reason):** 2 (gap 3 full-firewall half deferred §C
  research-gated; gap 7 microVM axis rejected §F; gap 8's already-built firewall is §E anti-phantom).
- **DROPPED (real misses — not in synthesis, not in design, not consciously cut):** 2 —
  (i) gap 6, the per-task wall-clock timeout / SIGTERM-and-commit runaway control for `/queue`
  (the cluster only mentioned it to say session-end ≠ timeout; no §C registration, no build row);
  (ii) gap 5's policy half — the "agent verifies migration against local stack, human applies to prod"
  decision + ADR that pass-3 §(d) explicitly asked for. The `supabase db push` *block* is designed, but
  the *positive policy* (where does an overnight agent's migration actually get verified, and the ADR
  recording that) is encoded nowhere.

Note: gaps 5 and 6 are both "policy/operational" rather than the article's strongest "build" gap (the
destructive-SQL guard, fully APPLIED). The two DROPS are the article's secondary contributions, not its core.
